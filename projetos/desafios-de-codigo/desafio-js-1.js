const numTotal = parseInt(gets())
const numFigCompradas = parseInt(gets())
const setFig = new Set()

for (figura of numFigCompradas) {
  //   const fig = parseInt(gets())
  //   setFig.add(fig)
}

print(numTotal - setFig.size)
